import 'package:flutter/material.dart';

class LatestPromos extends StatelessWidget {
  const LatestPromos({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
